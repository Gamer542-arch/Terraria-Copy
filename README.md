You need to enter your nickname in the code.
The .Venv files can be copied over, so installation is not necessary.
To run it, run py scripy.py.
If it doesn't work, reopen the cmd window (Right-click and open in terminal) in the folder where the script is located.

The script stops when you press ctrl + shift + tab, and after waiting 3 seconds, the script stops running. You can also close the script window.
It is adapted for YouTube.com.

Enjoy using it.
